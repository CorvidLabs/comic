schema_version = 1
profile = "standard"
[lifecycle]
command = ["fledge", "lanes", "run", "verify"]
[contract]
enabled = true
require_coverage = 0
skip_reason = "No canonical spec exists; deterministic content validation and SDD lifecycle enforcement remain blocking"
[risk]
threshold = "block"
[provenance]
mode = "soft"
policy = ".attest.json"
skip_reason = ""
[atlas]
enabled = false
skip_reason = "GitHub Pages deployment remains independently managed"
